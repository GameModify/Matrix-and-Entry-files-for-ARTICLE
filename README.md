Порог во всех расчетах: `0.05`.

Структура каждого варианта:

- `input/input.xlsx` — входной файл в формате основного загрузчика программы (`Decision Matrix`, `Preferences`);
- `tolerance_0_05/master_analysis.xlsx` — штатный отчет (находится в каждом эксперименте`;
- `summary.xlsx` — краткая сводка по всем экспериментам.

